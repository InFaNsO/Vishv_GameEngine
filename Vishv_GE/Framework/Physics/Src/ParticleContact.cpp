#include "Precompiled.h"
#include "ParticleContact.h"
#include "Particle.h"

/*void Vishv::Physics::ParticleContact::Resolve(float deltaTime)
{
	ResolveVelocity(deltaTime);
}

float Vishv::Physics::ParticleContact::CalculateSeperatingVelocity() const
{
	Math::Vector4 relVel(mParticles[0]->GetVelocity());
	if (mParticles[1])
		relVel -= mParticles[1]->GetVelocity();
	return  Math::Dot(relVel , mContactNormal);
}


void Vishv::Physics::ParticleContact::ResolveVelocity(float deltaTime)
{
	float sepVel = CalculateSeperatingVelocity();
	if (sepVel > 0.0f)
	{
		return;
	}


}*/
